import test from"node:test";import assert from"node:assert/strict";import{mkdtempSync,writeFileSync}from"node:fs";import{tmpdir}from"node:os";import{join}from"node:path";import{SyntaxEngine}from"../src/engine.js";
test("indexes content, ranks current and detects duplicates",()=>{const dir=mkdtempSync(join(tmpdir(),"syntax-test-")),a=join(dir,"Widget Master Register.md"),b=join(dir,"Widget OLD backup.md");writeFileSync(a,"canonical widget register for extension intelligence");writeFileSync(b,"canonical widget register for extension intelligence");const e=new SyntaxEngine(join(dir,"syntax.db"));assert.equal(e.indexFile(a),true);assert.equal(e.indexFile(b),true);const hits=e.search("widget register");assert.equal(hits.length,2);assert.equal(hits[0].state,"CURRENT");assert.equal(hits[1].state,"DUPLICATE");e.close()});
